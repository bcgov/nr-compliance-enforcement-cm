type Investigation {
  investigationGuid: String
  description: String
  leadAgency: String
  investigationStatus: InvestigationStatusCode
  openedTimestamp: Date
}

type Query {
  getInvestigation(investigationGuid: String): Investigation
}
