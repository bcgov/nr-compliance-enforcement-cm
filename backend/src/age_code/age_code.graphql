type AgeCode {
  age_code: String
  short_description: String
  long_description: String
  display_order: Int
  active_ind: Boolean
}

type Query {
  getAllAgeCodes: [AgeCode]!
  getAgeCode(age_code: String): AgeCode
}

