type AgeCode {
  age_code: String
  short_description: String
  long_description: String
  display_order: Int
}

type Query {
  getAllActiveAgeCodes: [AgeCode]!
  getAgeCode(age_code: String): AgeCode
}

