type SexCode {
  sex_code: String
  short_description: String
  long_description: String
  display_order: Int
}

type Query {
  getAllActiveSexCodes: [SexCode]!
  getSexCode(sex_code: String): SexCode
}

