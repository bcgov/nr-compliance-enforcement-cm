type SexCode {
  sex_code: String
  short_description: String
  long_description: String
  display_order: Int
  active_ind: Boolean
}

type Query {
  getAllSexCodes: [SexCode]!
  getSexCode(sex_code: String): SexCode
}

